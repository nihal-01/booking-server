const axios = require("axios");

module.exports = {
    getAvailability: async (
        from,
        to,
        departureDate,
        noOfAdults,
        noOfChildrens,
        noOfInfants
    ) => {
        try {
        } catch (err) {
            throw err;
        }
    },
};
