const sendErrorResponse = require("./sendErrorResponse");
const sendEmail = require("./sendEmail");
const sendAdminPassword = require("./sendAdminPassword");

module.exports = { sendErrorResponse, sendEmail, sendAdminPassword };
