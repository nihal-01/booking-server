const xml2js = require("xml2js");

module.exports = {
    getAgentTicketsRequest: async () => {
        try {
            // const response = await axios.get(`/`)
        } catch (err) {
            throw err;
        }
    },
};
